apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-tutorial.fullname" . }}
  labels:
    {{- include "aks-tutorial.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 8888
      nodePort: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-tutorial.selectorLabels" . | nindent 4 }}
